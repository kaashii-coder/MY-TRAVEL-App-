import 'package:flutter/material.dart';

class JourneynotesPage extends StatelessWidget {
  const JourneynotesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('No data'),),);
  }
}